# 唯一的必需依赖：图形窗口与输入事件。
# Python 3.14 目前没有官方 pygame wheel，可以改装 pygame-ce
# （社区分支，API 兼容，装完同样是 import pygame）：
#   pip install pygame-ce
pygame>=2.1

# numpy 是可选的：只在合成音效（方波/正弦波包络）时用到。
# 缺失时游戏会整体降级为静音，不会崩。想要音效就取消下面这行的注释。
# numpy>=1.24
